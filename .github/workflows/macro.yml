name: Prueba de Supervivencia
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Saludo
        run: echo "Hola Krista, si ves esto en verde, el robot sí tiene permiso de existir"
      
      - name: Probar Python
        run: python --version
